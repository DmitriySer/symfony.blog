{% extends 'base.html.twig' %}

{% block title %}Hello PostDetailsController!{% endblock %}

{% block content %}
    <!-- Content -->
    <div class="container mt-5" id="content">
        <div class="row justify-content-center">
            <!-- Share buttons -->
            <div class="col-lg-1 text-left mb-3 fixed" id="social-share">
                <a class="btn  btn-light m-2" href="#"><i class="fab fa-facebook-f"></i></a>
                <a class="btn  btn-light m-2" href="#"><i class="fab fa-google"></i></a>
                <a class="btn  btn-light m-2" href="#"><i class="fab fa-twitter"></i></a>
            </div>

            <!-- the content -->
            <div class="col-xl-7 col-lg-10 col-md-12">
                <h1>{{ post.getTitle() }}</h1>
                <div class="author-meta">
                    <span class="author-name">{{ post.getAuthor() }}</span>
                    <span class="author-tag">{{ post.getTopic() }}</span>
                </div>
                <p>{{ post.getContent() | nl2br }}</p>
            </div>

            <div class="col-lg-10 mt-3">
                <hr>
            </div>
        </div>
    </div>
{% endblock %}
